package net.xdabi.flappy.engine.input;

import static org.lwjgl.glfw.GLFW.*;


// Currently only the needed keyCodes are found in the enum
public class KeyCode {
    public static final int SPACE = GLFW_KEY_SPACE;
}
